we are doing an MVP holochain implmentation of https://farmhack.org with the original repo available for reference locally at ../farmhack-orig

This repo is a modified version of ../emergence 

IMPORTANT DEV INSTRUCTIONS:

- **nix for deps**: when running cargo build/test or other npm scripts allways use `nix develop -c` to load dependencies

- **Commit hygiene**: No claude co-authored messages. Use `npm` for builds. Run `nix develop` for dependencies.

- **Communication style**: No emotional tags or exclamation points. Just code-related information.
